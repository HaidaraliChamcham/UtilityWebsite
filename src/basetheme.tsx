import { createTheme } from '@mui/material/styles';

const basetheme = createTheme({
 typography: {
   h4: {
     fontWeight: 600,
   },
   h6: {
    fontWeight: 500,
  }
 }
})


export default basetheme;